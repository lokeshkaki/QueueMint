{
  "name": "@queuemint/dlq-monitor",
  "version": "0.1.0",
  "description": "DLQ Monitor Lambda - Polls and enriches DLQ messages",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "package": "npm run build && cd dist && zip -r ../dlq-monitor.zip ."
  },
  "keywords": ["lambda", "dlq", "sqs", "monitor"],
  "private": true,
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.478.0",
    "@aws-sdk/client-eventbridge": "^3.478.0",
    "@aws-sdk/client-sqs": "^3.478.0",
    "@aws-sdk/lib-dynamodb": "^3.478.0",
    "@queuemint/shared": "*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.6",
    "aws-sdk-client-mock": "^4.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  }
}
